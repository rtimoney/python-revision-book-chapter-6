# Storing Functions

# allows us to call funtions from the cat module
import cat


# will use the default for pet 
cat.lick()
cat.nap()
cat.purr()

# overrides the default param
cat.lick('Kitty')
cat.nap('Kitty')
cat.purr('Kitty')

